service:
  name: order-service
  host: 0.0.0.0
  port: 50003

database:
  # SQLite for test, PostgreSQL for production
  url: "sqlite:data/order_service.db"
  max_connections: 10

# gRPC target for Matching Engine
matching_engine:
  host: "127.0.0.1"
  port: 50009